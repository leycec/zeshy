#!/usr/bin/env zsh
# vi: filetype=zsh
# ====================[ calendar                           ]====================
#
# --------------------( LICENSE                            )--------------------
# Copyright 2007-2015 by Cecil Curry.
# See "LICENSE" for additional details.
#
# --------------------( SYNOPSIS                           )--------------------
# Handle calendars (i.e., well-formatted month day tables).

# ....................{ PRINTERS                           }....................
#FIXME: Revise to accept a 1-based month index to use as the quarter "center."
declare_function '
string print_calendar_quarter()

Print a Gregorian calendar of the current "quarter" (i.e., previous, current,
and next month).
'
function print_calendar_quarter() {
    die_if_args
    cal -3
}

declare_function '
string print_calendar_year(integer year = $(get_time_year))

Print a Gregorian calendar of the passed year (defaulting to the current).
'
function print_calendar_year() {
    die_unless_args_0_to_1 'expected optional year'
    string year; year="${1:-$(get_time_year)}"
    :int.die_unless "${year}"
    cal -y "${year}"
}

# --------------------( WASTELANDS                         )--------------------
