#!/usr/bin/env zsh
# ====================[ driconf                            ]====================
#
# --------------------( LICENSE                            )--------------------
# Copyright 2007-2013 by Cecil Curry.
# See "COPYING" for additional details.
#
# --------------------( SYNOPSIS                           )--------------------
# Handle "driconf", a GUI for configuring the Direct Rendering Infrastructure
# (DRI) of the current OpenGL driver (e.g., Mesa Gallium).

# ....................{ CONFIGURERS                        }....................
# void configure_mesa_dri(void)
#
# Configure the current OpenGL driver's Direct Rendering Infrastructure (DRI).
function configure_mesa_dri() {
    die_if_args
    run_command_disowned driconf
}

# --------------------( WASTELANDS                         )--------------------
