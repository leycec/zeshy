#!/usr/bin/env zsh
# --------------------( LICENSE                            )--------------------
# Copyright 2007-2013 by Cecil Curry.
# See "COPYING" for additional details.

declare_parcel_as_script_with_stdin <<'-/-'
Handle core variable functionality.
-/-

# ....................{ PRINTERS                           }....................
declare_function_with_stdin <<'/---'
string print_variable(string variable_name1, string variable_name2, ...)

Print the passed variables in human-readable format containing both the type and
value of such variables.
/---
function print_variable() {
    #FIXME: While fine, such output is rather bland and difficult to read. For
    #readability, lightly parse and colorify such output. See
    #print_aliases_cli() for similar code.

    # Validate sanity.
    die_unless_args 'Expected one or more variable names.'

    # Print such variables.
    typeset -p -- "${@}" | page_stdin
}

# --------------------( WASTELANDS                         )--------------------
# ....................{ PRINTERS                           }....................
#declare_function_with_stdin <<'/---'
#string print_variable(string variable_name)
#
#Print a human-readable synopsis of the passed variable.
#/---
#function print_variable() {
#    # Validate sanity.
#    die_unless_arg 'expected one variable name'
#    string variable_name__pv="${1}"
#    die_unless_variable "${variable_name__pv}"
#
#    #FUXME: Obviously, a mediocre try. But this will have to do, for the moment!
#    # Print such variable.
#    output_string "${(P)variable_name__pv}"
#}

#   output_string "${(P)variable_name__pv}"
