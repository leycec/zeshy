#!/usr/bin/env zsh
# ====================[ posix                              ]====================
#
# --------------------( SYNOPSIS                           )--------------------
# Configure common external commands. Most such commands are POSIX-mandated
# (e.g., "awk", "sed", "grep"); the remainder are merely sufficiently
# commonplace to be effectively mandated (e.g., "less", "mount").

#FIXME: Split into two files "if_config/option/builtin" and
#"if_config/option/posix".

# ....................{ CD                                 }....................
declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_CD_OPTIONS

`cd` options specific to the set_dir*() functions.
________________<heredoc?>________________
ZESHY_CD_OPTIONS=()

declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_CD_OPTIONS_IF_INTERACTIVE

`cd` options if run interactively.
________________<heredoc?>________________
ZESHY_CD_OPTIONS_IF_INTERACTIVE=()

declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_CD_OPTIONS_IF_NONINTERACTIVE

`cd` options if run non-interactively.
________________<heredoc?>________________
ZESHY_CD_OPTIONS_IF_NONINTERACTIVE=(
    # If non-interactive, change directories idempotently (i.e., without
    # hidden side effects) by not implicitly calling builtin chpwd() or any
    # functions registered with run_hook_after_dir_set_interactively(). See
    # set_dir() for further details.
    -q
)

declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_CD_OPTIONS_IF_IDEMPOTENT

`cd` options if run idempotently (i.e., without inducing side effects by calling
hook functions). See set_dir_idempotently() for further details.
________________<heredoc?>________________
ZESHY_CD_OPTIONS_IF_IDEMPOTENT=(
)

# ....................{ CHMOD                              }....................
declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_CHMOD_OPTIONS

`chmod` options specific to the set_path_permissions*() functions.
________________<heredoc?>________________
ZESHY_CHMOD_OPTIONS=()

declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_CHMOD_OPTIONS_IF_INTERACTIVE

`chmod` options if run interactively.
________________<heredoc?>________________
ZESHY_CHMOD_OPTIONS_IF_INTERACTIVE=(
    --changes
)

declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_CHMOD_OPTIONS_IF_NONINTERACTIVE

`chmod` options if run non-interactively.
________________<heredoc?>________________
ZESHY_CHMOD_OPTIONS_IF_NONINTERACTIVE=()

declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_CHMOD_OPTIONS_IF_RECURSIVE

`chmod` options if run recursively.
________________<heredoc?>________________
ZESHY_CHMOD_OPTIONS_IF_RECURSIVE=(
    --preserve-root
    --recursive
)

# ....................{ CHOWN                              }....................
declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_CHOWN_OPTIONS

`chown` options specific to the set_path_user_and_group*() functions.
________________<heredoc?>________________
ZESHY_CHOWN_OPTIONS=()

declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_CHOWN_OPTIONS_IF_INTERACTIVE

`chown` options if run interactively.
________________<heredoc?>________________
ZESHY_CHOWN_OPTIONS_IF_INTERACTIVE=(
    --changes
)

declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_CHOWN_OPTIONS_IF_NONINTERACTIVE

`chown` options if run non-interactively.
________________<heredoc?>________________
ZESHY_CHOWN_OPTIONS_IF_NONINTERACTIVE=()

declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_CHOWN_OPTIONS_IF_RECURSIVE

`chown` options if run recursively.
________________<heredoc?>________________
ZESHY_CHOWN_OPTIONS_IF_RECURSIVE=(
    --preserve-root
    --recursive
)

# ....................{ CP                                 }....................
declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_CP_OPTIONS

`cp` options specific to the copy_path*() functions.
________________<heredoc?>________________
ZESHY_CP_OPTIONS=(
    --archive
)

declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_CP_OPTIONS_IF_INTERACTIVE

`cp` options if run interactively.
________________<heredoc?>________________
ZESHY_CP_OPTIONS_IF_INTERACTIVE=(
    --interactive
    --verbose
)

declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_CP_OPTIONS_IF_NONINTERACTIVE

`cp` options if run non-interactively.
________________<heredoc?>________________
ZESHY_CP_OPTIONS_IF_NONINTERACTIVE=()

declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_CP_OPTIONS_IF_RECURSIVE

`cp` options if run recursively.
________________<heredoc?>________________
ZESHY_CP_OPTIONS_IF_RECURSIVE=(
    --recursive
    --update
)

declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_CP_OPTIONS_IF_DEEP

`cp` options if copying deeply (i.e., copying the targets of source symbolic
links rather than such links).
________________<heredoc?>________________
ZESHY_CP_OPTIONS_IF_DEEP=(
    --dereference
)

# ....................{ DIFF                               }....................
declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_DIFF_OPTIONS

`diff` options specific to the difference_files*() and make_file_patch*()
functions.
________________<heredoc?>________________
ZESHY_DIFF_OPTIONS=(
    --new-file
    --speed-large-files
    --unified
)

declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_DIFF_OPTIONS_IF_INTERACTIVE

`diff` options if run interactively.
________________<heredoc?>________________
# Ignoring whitespace is nice when run interactively but otherwise dangerous.
ZESHY_DIFF_OPTIONS_IF_INTERACTIVE=(
    --ignore-all-space
    --ignore-blank-lines
    --ignore-space-change
)

declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_DIFF_OPTIONS_IF_NONINTERACTIVE

`diff` options if run non-interactively.
________________<heredoc?>________________
ZESHY_DIFF_OPTIONS_IF_NONINTERACTIVE=()

declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_DIFF_OPTIONS_IF_RECURSIVE

`diff` options if run recursively.
________________<heredoc?>________________
ZESHY_DIFF_OPTIONS_IF_RECURSIVE=(
    --recursive
)

# ....................{ DU                                 }....................
declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_DU_OPTIONS

`du` options specific to the print_path_size*() functions.
________________<heredoc?>________________
ZESHY_DU_OPTIONS=(
    --one-file-system
)

declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_DU_OPTIONS_IF_INTERACTIVE

`du` options if run interactively.
________________<heredoc?>________________
ZESHY_DU_OPTIONS_IF_INTERACTIVE=(
    --human-readable
    --total
)

declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_DU_OPTIONS_IF_NONINTERACTIVE

`du` options if run non-interactively.
________________<heredoc?>________________
ZESHY_DU_OPTIONS_IF_NONINTERACTIVE=()

declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_DU_OPTIONS_IF_GETTING_DIR_SIZE

`du` options if getting the size in bytes of a single directory.
________________<heredoc?>________________
ZESHY_DU_OPTIONS_IF_GETTING_DIR_SIZE=(
    --summary
    --block-size=1
)

# ....................{ DU ~ sort                          }....................
declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_DU_SORT_OPTIONS

`sort` options specific to the print_path_size_sorted*() functions.
________________<heredoc?>________________
ZESHY_DU_SORT_OPTIONS=(
    --human-numeric-sort
    --reverse
)

# ....................{ FILE                               }....................
declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_FILE_OPTIONS

`file` options specific to the *_file_*() functions.
________________<heredoc?>________________
ZESHY_FILE_OPTIONS=()

declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_FILE_OPTIONS_IF_PRINTING_METADATA_SYNOPSIS

`file` options if printing a human-readable synopsis of file metadata.
________________<heredoc?>________________
ZESHY_FILE_OPTIONS_IF_PRINTING_METADATA_SYNOPSIS=(
    --dereference
)

# ....................{ GREP                               }....................
# Avoid defining global ${GREP_OPTION}. Defining such global coerces such
# options on all invocations of `grep`, including invocation by shell scripts
# expecting machine- rather than human-readable print_string (e.g., no line numbers or
# colors). To avoid breaking the entire system, avoid such global.

declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_GREP_OPTIONS

`grep` options specific to the grep*() functions.
________________<heredoc?>________________
ZESHY_GREP_OPTIONS=(
    --binary-files=without-match
    --exclude-dir='.cvs'
    --exclude-dir='.git'
    --exclude-dir='.hg'
    --exclude-dir='.svn'
    --no-messages
    --perl-regexp
)

declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_GREP_OPTIONS_IF_INTERACTIVE

`grep` options if run interactively.
________________<heredoc?>________________
ZESHY_GREP_OPTIONS_IF_INTERACTIVE=(
    --color=always
    --line-number
    --with-filename
)

declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_GREP_OPTIONS_IF_NONINTERACTIVE

`grep` options if run non-interactively.
________________<heredoc?>________________
ZESHY_GREP_OPTIONS_IF_NONINTERACTIVE=()

declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_GREP_OPTIONS_IF_CASELESS

`grep` options if run case-insensitively.
________________<heredoc?>________________
ZESHY_GREP_OPTIONS_IF_CASELESS=(
    --ignore-case
)

declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_GREP_OPTIONS_IF_RECURSIVE

`grep` options if run recursively.
________________<heredoc?>________________
ZESHY_GREP_OPTIONS_IF_RECURSIVE=(
    --recursive
)

declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_GREP_OPTIONS_IF_SILENT

`grep` options if run silently.
________________<heredoc?>________________
ZESHY_GREP_OPTIONS_IF_SILENT=(
    --silent
)

# ....................{ LESS                               }....................
declare_global_with_stdin <<'________________<heredoc?>________________'
string LESS

Whitespace-delimited `less` options. Such options are not specific to Zeshy and
hence applied to all `less` invocations, particularly those by external commands
(e.g., `man`).
________________<heredoc?>________________
LESS=`${LESS--R}`

declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_LESS_OPTIONS_IF_INTERACTIVE

`less` options specific to the page_safely() function.
________________<heredoc?>________________
ZESHY_LESS_OPTIONS_IF_INTERACTIVE=(
    # Display the current pager position in the `prompt` (i.e., the last line).
    --LONG-PROMPT

    # Display raw control characters, excluding ANSI escape sequences.
    --RAW-CONTROL-CHARS

    # Avoid clearing the screen on initialization.
    --no-init

    # When scrolling horizontally, `shift` the screen by a number of columns
    # equal to %5 of the current terminal width.
    --shift=.05
)

# ....................{ LN ~ symbolic                      }....................
declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_LN_SYMBOLIC_OPTIONS

`ln` options specific to the make_link_symbolic*() functions.
________________<heredoc?>________________
ZESHY_LN_SYMBOLIC_OPTIONS=(
    --symbolic
)

declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_LN_SYMBOLIC_OPTIONS_IF_INTERACTIVE

`ln` options if run interactively.
________________<heredoc?>________________
ZESHY_LN_SYMBOLIC_OPTIONS_IF_INTERACTIVE=(
    --interactive
    --verbose
)

declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_LN_SYMBOLIC_OPTIONS_IF_NONINTERACTIVE

`ln` options if run non-interactively.
________________<heredoc?>________________
ZESHY_LN_SYMBOLIC_OPTIONS_IF_NONINTERACTIVE=()

# ....................{ LS                                 }....................
declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_LS_OPTIONS

`ls` options specific to the list_path*() functions.
________________<heredoc?>________________
ZESHY_LS_OPTIONS=()

declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_LS_OPTIONS_IF_INTERACTIVE

`ls` options if run interactively.
________________<heredoc?>________________
ZESHY_LS_OPTIONS_IF_INTERACTIVE=(
    --all
    --color=always
    --group-directories-first
    --human-readable
    --indicator-style=classify
    --quoting-style=shell
    --sort=version
    --time-style=long-iso
)

declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_LS_OPTIONS_IF_NONINTERACTIVE

`ls` options if run non-interactively.
________________<heredoc?>________________
ZESHY_LS_OPTIONS_IF_NONINTERACTIVE=()

declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_LS_OPTIONS_IF_CONCISE

`ls` options if run concisely (i.e., non-verbosely).
________________<heredoc?>________________
ZESHY_LS_OPTIONS_IF_CONCISE=(
    --format=vertical
)

declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_LS_OPTIONS_IF_RECURSIVE

`ls` options if run recursively.
________________<heredoc?>________________
ZESHY_LS_OPTIONS_IF_RECURSIVE=(
    --format=long
    --recursive
)

declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_LS_OPTIONS_IF_VERBOSE

`ls` options if run verbosely.
________________<heredoc?>________________
ZESHY_LS_OPTIONS_IF_VERBOSE=(
    --format=long
)

# ....................{ MKDIR                                 }....................
declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_MKDIR_OPTIONS

`mkdir` options specific to the make_dir*() functions.
________________<heredoc?>________________
ZESHY_MKDIR_OPTIONS=()

declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_MKDIR_OPTIONS_IF_INTERACTIVE

`mkdir` options if run interactively.
________________<heredoc?>________________
ZESHY_MKDIR_OPTIONS_IF_INTERACTIVE=(
    --verbose
)

declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_MKDIR_OPTIONS_IF_NONINTERACTIVE

`mkdir` options if run non-interactively.
________________<heredoc?>________________
ZESHY_MKDIR_OPTIONS_IF_NONINTERACTIVE=()

declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_MKDIR_OPTIONS_IF_NOT_FOUND

`mkdir` options if ignoring nonextant directories.
________________<heredoc?>________________
ZESHY_MKDIR_OPTIONS_IF_NOT_FOUND=(
    --parent
)

# ....................{ MV                                 }....................
declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_MV_OPTIONS

`mv` options specific to the move_path*() functions.
________________<heredoc?>________________
ZESHY_MV_OPTIONS=()

declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_MV_OPTIONS_IF_INTERACTIVE

`mv` options if run interactively.
________________<heredoc?>________________
ZESHY_MV_OPTIONS_IF_INTERACTIVE=(
    --interactive
    --verbose
)

declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_MV_OPTIONS_IF_NONINTERACTIVE

`mv` options if run non-interactively.
________________<heredoc?>________________
ZESHY_MV_OPTIONS_IF_NONINTERACTIVE=()

# ....................{ NL                                 }....................
declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_NL_OPTIONS

`nl` options specific to the number_string_lines*() functions.
________________<heredoc?>________________
ZESHY_NL_OPTIONS=(
    -ba
)

declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_NL_OPTIONS_IF_INTERACTIVE

`nl` options if run interactively.
________________<heredoc?>________________
ZESHY_NL_OPTIONS_IF_INTERACTIVE=()

declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_NL_OPTIONS_IF_NONINTERACTIVE

`nl` options if run non-interactively.
________________<heredoc?>________________
ZESHY_NL_OPTIONS_IF_NONINTERACTIVE=()

# ....................{ NM                                 }....................
declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_NM_OPTIONS

`nm` options specific to the print_binary_executable_symbols() function.
________________<heredoc?>________________
ZESHY_NM_OPTIONS=(
)

declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_NM_OPTIONS_IF_INTERACTIVE

`nm` options if run interactively.
________________<heredoc?>________________
ZESHY_NM_OPTIONS_IF_INTERACTIVE=(
    --demangle
    --line-numbers
    --print-armap
    --print-size
)

declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_NM_OPTIONS_IF_NONINTERACTIVE

`nm` options if run non-interactively.
________________<heredoc?>________________
ZESHY_NM_OPTIONS_IF_NONINTERACTIVE=()

declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_NM_OPTIONS_IF_BINARY_EXECUTABLE_DYNAMIC

`nm` options if passed a dynamically linked executable binary.
________________<heredoc?>________________
ZESHY_NM_OPTIONS_IF_BINARY_EXECUTABLE_DYNAMIC=(
    --dynamic
)

# ....................{ PATCH                               }....................
declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_PATCH_OPTIONS

`patch` options specific to the apply_file_patch*() functions.
________________<heredoc?>________________
ZESHY_PATCH_OPTIONS=(
    --no-backup-if-mismatch
    --remove-empty-files
)

declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_PATCH_OPTIONS_IF_INTERACTIVE

`patch` options if run interactively.
________________<heredoc?>________________
ZESHY_PATCH_OPTIONS_IF_INTERACTIVE=()

declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_PATCH_OPTIONS_IF_NONINTERACTIVE

`patch` options if run non-interactively.
________________<heredoc?>________________
ZESHY_PATCH_OPTIONS_IF_NONINTERACTIVE=()

# ....................{ RM                                 }....................
declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_RM_OPTIONS

`rm` options specific to the remove_file*() functions.
________________<heredoc?>________________
ZESHY_RM_OPTIONS=(
    --one-file-system
    --preserve-root
)

declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_RM_OPTIONS_IF_INTERACTIVE

`rm` options if run interactively.
________________<heredoc?>________________
ZESHY_RM_OPTIONS_IF_INTERACTIVE=(
    --verbose
)

declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_RM_OPTIONS_IF_INTERACTIVE_AND_ARGS_1_TO_3

`rm` options if run interactively and passed only one to three paths to remove.
________________<heredoc?>________________
ZESHY_RM_OPTIONS_IF_INTERACTIVE_AND_ARGS_1_TO_3=(
    -i
)

declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_RM_OPTIONS_IF_INTERACTIVE_AND_ARGS_4_OR_MORE

`rm` options if run interactively and passed at least four paths to remove.
________________<heredoc?>________________
ZESHY_RM_OPTIONS_IF_INTERACTIVE_AND_ARGS_4_OR_MORE=(
    -I
)

declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_RM_OPTIONS_IF_NONINTERACTIVE

`rm` options if run non-interactively.
________________<heredoc?>________________
ZESHY_RM_OPTIONS_IF_NONINTERACTIVE=()

declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_RM_OPTIONS_IF_RECURSIVE

`rm` options if run recursively.
________________<heredoc?>________________
ZESHY_RM_OPTIONS_IF_RECURSIVE=(
    --recursive
)

# ....................{ RMDIR                                 }....................
declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_RMDIR_OPTIONS

`rmdir` options specific to the remove_dir*() functions.
________________<heredoc?>________________
ZESHY_RMDIR_OPTIONS=()

declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_RMDIR_OPTIONS_IF_INTERACTIVE

`rmdir` options if run interactively.
________________<heredoc?>________________
ZESHY_RMDIR_OPTIONS_IF_INTERACTIVE=(
    --verbose
)

declare_global_with_stdin <<'________________<heredoc?>________________'
list ZESHY_RMDIR_OPTIONS_IF_NONINTERACTIVE

`rmdir` options if run non-interactively.
________________<heredoc?>________________
ZESHY_RMDIR_OPTIONS_IF_NONINTERACTIVE=()
