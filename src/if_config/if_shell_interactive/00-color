#!/usr/bin/env zsh
# ====================[ 00-color                           ]====================
#
# --------------------( SYNOPSIS                           )--------------------
# Configure interactive colors.

# ....................{ COLORS                             }....................
document_global '
map ZESHY_COLOR_STYLE_NAME_TO_COLOR_NAME

Map color style names to color names. Color names are keys of map
${ZESHY_COLOR} expanding to the ANSI escape code enabling such color on the
current terminal. Color style names include:

/==============================v===============================================\
| color style name             | application                                   |
>==============================+===============================================<
| punctuation                  | punctuation with no semantic meaning          |
| punctuation_semantic         | punctuation with semantic meaning             |
>~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~+~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~<
| say_caller                   | caller name printed by say(), cry(), etc.     |
\==============================^===============================================/
'
ZESHY_COLOR_STYLE_NAME_TO_COLOR_NAME=(
    punctuation           grey
    punctuation_semantic  grey_bold
    say_caller            cyan
)
