#!/usr/bin/env zsh
# ====================[ mysql                              ]====================
#
# --------------------( SYNOPSIS                           )--------------------
# Zeshy MySQL configuration file.

# ....................{ MYSQL                              }....................
# mysql_shell()-specific MySQL shell options.
global_list ZESHY_MYSQL_SHELL_OPTIONS; ZESHY_MYSQL_SHELL_OPTIONS=(
    --pager=less
    --auto-rehash
    --column-names
    --compress
    --ignore-case
    --line-numbers
    --named-commands
    --no-beep
    --password
    --reconnect
    --secure-auth
    --show-warnings
    --sigint-ignore
    --skip-comments
    --table
)
