#!/usr/bin/env zsh

:parcel <<'/---'
Configure `useradd`, an external command adding a new local user.
/---

# ....................{ OPTIONS                            }....................
:list.global ZY_OPTIONS_USERADD <<'/---'
`useradd` options for `zeshy`-specific user adders (e.g., :make_user()).
/---
ZY_OPTIONS_USERADD=()

:list.global ZY_OPTIONS_USERADD_IF_INTERACTIVE <<'/---'
`useradd` options when run interactively.
/---
ZY_OPTIONS_USERADD_IF_INTERACTIVE=()

:list.global ZY_OPTIONS_USERADD_IF_NONINTERACTIVE <<'/---'
`useradd` options when run non-interactively.
/---
ZY_OPTIONS_USERADD_IF_NONINTERACTIVE=()
